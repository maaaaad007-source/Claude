# Only the brand colour is pinned. Background and text colours are left to
# Streamlit so the viewer's System / Light / Dark choice actually takes effect;
# setting them here would force one theme and make that control a no-op.
[theme]
primaryColor = "#6B9080"

[server]
enableXsrfProtection = true

[browser]
gatherUsageStats = false
